services:
  - type: worker
    name: vertex-bot
    runtime: python3.10
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: TOKEN
        sync: false
      - key: CHAT_ID
        sync: false
